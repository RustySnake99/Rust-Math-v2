# RustMath-v2
This project requires a virtual environment. You can create one using the following command:
`bash
python -m venv venv`

After creating the virtual environment, activate it using the appropriate command for your operating system:
- On Windows:
venv\Scripts\activate

Now run the python programs!

To deactivate the virtual environment, simply run:
deactivate
